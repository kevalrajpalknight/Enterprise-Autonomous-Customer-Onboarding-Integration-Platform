[project]
name = "shared"
version = "0.1.0"
description = "Shared utilities, logging, and observability helpers for OnboardAI"
requires-python = ">=3.12"
dependencies = [
    "boto3>=1.35.0",
    "structlog>=24.4.0",
    "prometheus-client>=0.21.0",
]
